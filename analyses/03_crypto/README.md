# Analysis 4.3 — Cryptographic Library Performance

This directory contains the preserved scripted analysis used to derive the cryptographic results. Run `python scripts/analyze_crypto.py` from a working directory containing the required raw CSV files or adapt the input paths in the script as needed.
