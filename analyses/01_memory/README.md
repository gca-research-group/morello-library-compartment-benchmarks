# Analysis 4.1 — Memory Performance

This directory contains the preserved scripted analysis used to derive the memory results. Run `python scripts/analyze_memory.py` from a working directory containing the required raw CSV files or adapt the input paths in the script as needed.
